import React from 'react';

const Layout = ({ children }) => {
    return (
        <div>
            {/* Layout principal à réintégrer */}
            {children}
        </div>
    );
};

export default Layout;
